services:
  autodrive-kb-rag:
    build:
        context: .
    image: autodrive-kb-rag
    container_name: autodrive-kb-rag-service
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped